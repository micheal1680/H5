设置浮动的本质，就是给元素设置“行内块级标签”，因为这样可以设置宽高，left和right都是独占一行，通过设置行内块级标签之后，可以放到
一行；(所以除了必要时，慎用浮动，因为还要清除浮动很麻烦，容易出现问题)
用浮动可以达到自己想要的效果，但是浮动会影响到其他的东西，因此需要清除浮动，但是所设置的效果依旧不变，还是自己想要的效果；
盒子类名都是box；
定位（position）:子绝父相（成对使用），相不脱离标准文档流，绝要脱离，多半使用固定和绝对，很少使用相；只要设置了定位，就有（z-index：数值），数值默认为零，这个是用来设置
层叠性，数值越大，越在上面；
一个网页在一个大的div里面，分为几个部分，就有几个div;
link是引入.css文件；
浮动和定位都可以用来定位，设置内容的位置；
line-height:可以用于设置文本垂直方向居中；
input的使用：根据type的不同而不同的input东西，若type为text,则为文本框，若为chekebox，则为选择小框；
转换为行内块级标签，内容有多宽，则框架就有多宽是，其所设的padding等也是参照这个边框来；
否定伪类选择器12种，是要常用的；
一定要把之前的难点知识点都用起来；
看老师文件的day5的弹性盒子，掌握定位，位置的确定；（auto自动）
弹性盒子的第一级子元素才能称为项目，且在一行显示即默认排成一行，即使排不下，依旧不会换行；
老师加上自己找相关的知识进行实践，多写多练；
便签之间本身有间隙，可以通过设置浮动消除间隙，寻找消除间隙的方法；
一定要分清楚项目属性和容器属性；
一般采用css文件来对网页进行修饰，因此有多少个css文件，就引入多少个link标签；
行内块级标签是由内容撑出宽度，内容有多少，宽度就有多少，因此不独占一行的不同标签写好之后，直接将标签转换为行内块级标签就行；
或者根据实际情况再进行修饰；
理解边偏移，到底是怎么移的，针对哪个移；
margin:0 auto;作用：水平居中（左右的外边距相同），但前提是要设宽度；
.box:hover+第一个兄弟选择器   =    .box:hover 后代的子代选择器；
如果要在前面添加内容，则可以采用.box:hover 后代的子代选择器::before
a标签不独占一行，将其转换为bloke标签，可以转换为一列；
下载字体：方正字体网站，笔记上也有网址；
字体图标，所有的字体属性它都可以运用；（比如设置字体图标的大小用font-size）;
在iconfont里面找图片，如果要直接使用在线图片啊，则可以复制其类名粘贴到class类名（空格）的后面；
行内标签不能旋转，因此需要改变其标签显示模式；
设定位时，标签会自动变为行内块级标签，宽高由其内容撑出，若其自身没有内容，则其会消失，因此解决方法是对其设宽高；
写css文件时，0.26直接写成.26；
可以用inline-bloke和弹性布局来实现一列变为一行；
overflow：auto；即多出的内容自动出现滚动条；
想要哪个占完剩余空间，就对哪个设flex：1；就相当于设flex grow:1;即占满剩余空间；过渡的使用也可以看这个文件，有几种类型目标都可以使用过渡；
移动端布局（6种布局方式，写时择其一，不能同时使用2中布局方式）时，要把meta的那一行代码写全（具体去看老师文件里面的day8复习文件，）
行内块级标签内的标签之间本来就有间隙，若要去掉间隙，则去掉行内块级标签形式，设置弹性布局；
了解三种标签的性质，关于间隙，独占一行，内容撑出宽度等的性质；
有一些形式设置不起作用，原因可能是浏览器不兼容，因此要做相关的设置，看老师文件的day08的05中的span处；
关于窗口的缩放之后，页面的内容不乱，可以自适应的方法，pc端与移动端都要掌握；
弹性盒子与定位可以同时设置，但是弹性盒子与浮动不能同时存在，会互相影响；
对于定位：left：正值：表示从左往右移动；left：负值：表示从右往左移动；
left：0表示离左边距离为0；right，top,bottom同理；
animate.css网站里面有很多写好的动画效果的css文件；结合Github网站（讲应用方法）叫你如何使用；
要用z-idex，首先得设定位；
标准盒子与
1.让盒子垂直剧中的方法：弹性布局；定位（设置边偏移）

